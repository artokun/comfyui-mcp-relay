{
  "name": "comfyui-mcp-relay",
  "private": true,
  "version": "0.1.0",
  "description": "WebSocket relay (Cloudflare Worker + Durable Objects) for comfyui-mcp's secure remote-pod bridge — replaces ephemeral cloudflared quick tunnels with a stable, valid-TLS wss:// endpoint.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
